# Enhanced Subtitle Temporary Storage Type System\n\nComprehensive TypeScript type definitions for the enhanced subtitle auto-save system with advanced type safety, validation utilities, and developer experience improvements.\n\n## Overview\n\nThe enhanced subtitle temporary storage type system provides:\n\n- **Type Safety**: Comprehensive TypeScript types with branded identifiers\n- **Validation**: Runtime type checking with detailed error reporting\n- **Performance**: Optimized caching and operation tracking\n- **Integration**: Seamless integration with existing workspace system\n- **Developer Experience**: Rich JSDoc documentation and usage examples\n\n## File Structure\n\n```\ngui/src/renderer/src/types/\n├── subtitle-temp-storage.ts              # Core temporary storage types\n├── subtitle-temp-storage-utils.ts        # Advanced utilities and type guards\n├── subtitle-temp-storage-integration.ts  # Integration with workspace system\n└── subtitle-temp-storage-examples.ts     # Usage examples and documentation\n```\n\n## Core Features\n\n### 1. Enhanced Type Safety\n\n#### Branded Types\nCritical identifiers are \"branded\" to prevent mixing different ID types:\n\n```typescript\nexport type WorkspaceId = Brand<string, 'WorkspaceId'>\nexport type SessionId = Brand<string, 'SessionId'>\nexport type StorageId = Brand<string, 'StorageId'>\nexport type OperationId = Brand<string, 'OperationId'>\n```\n\n#### Generic Operation Results\nType-safe operation results with consistent error handling:\n\n```typescript\nexport type SubtitleTempResult<T> = {\n  success: true\n  data: T\n  metadata?: Partial<SubtitleTempMetadata>\n  metrics: SubtitleTempOperationResponse['metrics']\n} | {\n  success: false\n  error: SubtitleTempError\n  metrics: SubtitleTempOperationResponse['metrics']\n}\n```\n\n### 2. Advanced Validation System\n\n#### Comprehensive Type Guards\nRobust type guards with deep validation:\n\n```typescript\nexport function isSubtitleTempContent(content: any): content is SubtitleTempContent {\n  if (!content || typeof content !== 'object') return false\n  \n  return (\n    content.metadata &&\n    typeof content.metadata.id === 'string' &&\n    typeof content.metadata.workspaceId === 'string' &&\n    Array.isArray(content.subtitles) &&\n    // ... additional validation\n  )\n}\n```\n\n#### Validation Rules Engine\nComposable validation rules with detailed feedback:\n\n```typescript\nexport interface ValidationRule<T> {\n  name: string\n  validator: (value: T, context?: any) => ValidationResult\n  severity: ValidationSeverity\n  async?: boolean\n  dependsOn?: string[]\n}\n```\n\n### 3. Performance Monitoring\n\n#### Performance Tracker Interface\nBuilt-in performance monitoring for all operations:\n\n```typescript\nexport interface PerformanceTracker {\n  startOperation(name: string, context?: OperationContext): string\n  endOperation(id: string): number\n  recordMetric(name: string, value: number, tags?: Record<string, string>): void\n  getMetrics(filter?: string): PerformanceMetric[]\n  getAggregatedMetrics(operation: string): AggregatedMetrics\n}\n```\n\n#### Metrics Collection\nDetailed metrics for optimization and monitoring:\n\n```typescript\nexport interface PerformanceMetric {\n  id: string\n  operation: string\n  value: number\n  unit: 'ms' | 'bytes' | 'count' | 'ratio' | 'ops/sec'\n  timestamp: number\n  tags?: Record<string, string>\n  context?: OperationContext\n}\n```\n\n### 4. React Hook Integration\n\n#### Enhanced Hook Options\nComprehensive configuration for React integration:\n\n```typescript\nexport interface UseSubtitleTempStorageOptions {\n  workspaceId: WorkspaceId\n  sessionConfig?: {\n    sessionType: SubtitleTempSession['sessionType']\n    autoStart: boolean\n    inheritFromPrevious: boolean\n  }\n  autoSave?: Partial<SubtitleAutoSaveConfig>\n  sync?: {\n    enabled: boolean\n    direction: 'temp-to-persistent' | 'persistent-to-temp' | 'bidirectional'\n    interval: number\n    conflictResolution: 'prefer-temp' | 'prefer-persistent' | 'manual'\n  }\n  // ... additional options\n}\n```\n\n#### Hook Return Type\nRich hook interface with operations, state, and utilities:\n\n```typescript\nexport interface UseSubtitleTempStorageResult {\n  // Session management\n  session: SubtitleTempSession | null\n  sessionId: SessionId | null\n  isSessionActive: boolean\n  \n  // Content management\n  content: SubtitleTempContent | null\n  hasUnsavedChanges: boolean\n  changeCount: number\n  \n  // Operations\n  operations: {\n    saveContent: (content: SubtitleTempContent) => Promise<void>\n    loadContent: (sessionId?: SessionId) => Promise<SubtitleTempContent | null>\n    createSession: (sessionType?: string) => Promise<SessionId>\n    synchronize: (options?: Partial<SynchronizationOptions>) => Promise<SynchronizationResult>\n    // ... additional operations\n  }\n  \n  // State management\n  state: {\n    isLoading: boolean\n    isSaving: boolean\n    error: SubtitleTempError | null\n  }\n  \n  // Utilities\n  utils: {\n    validateContent: (content: SubtitleTempContent) => ValidationResult\n    estimateStorageSize: (content: SubtitleTempContent) => number\n    clearCache: () => Promise<void>\n    // ... additional utilities\n  }\n}\n```\n\n### 5. Storage Bridge System\n\n#### Bridge Interface\nSeamless integration between temporary and persistent storage:\n\n```typescript\nexport interface SubtitleStorageBridge {\n  toPersistentContent(tempContent: SubtitleTempContent): SubtitleFileContent\n  toTemporaryContent(fileContent: SubtitleFileContent, sessionId: SessionId): SubtitleTempContent\n  synchronize(\n    workspaceId: WorkspaceId,\n    sessionId: SessionId,\n    options?: SynchronizationOptions\n  ): Promise<TypedOperationResult<SynchronizationResult>>\n  validateCompatibility(\n    tempContent: SubtitleTempContent,\n    fileContent: SubtitleFileContent\n  ): ValidationResult\n}\n```\n\n#### Synchronization Results\nDetailed feedback from synchronization operations:\n\n```typescript\nexport interface SynchronizationResult {\n  operations: Array<{\n    type: 'create' | 'update' | 'delete' | 'merge'\n    target: 'temporary' | 'persistent'\n    itemId: string\n    success: boolean\n    error?: string\n  }>\n  conflicts: Array<{\n    itemId: string\n    field: string\n    tempValue: any\n    persistentValue: any\n    resolution: 'temp' | 'persistent' | 'merged' | 'manual'\n    timestamp: number\n  }>\n  metrics: {\n    itemsProcessed: number\n    conflictsResolved: number\n    duration: number\n    bytesTransferred: number\n  }\n}\n```\n\n## Usage Examples\n\n### Basic Session Creation\n\n```typescript\nimport { \n  createWorkspaceId, \n  createSessionId, \n  isSubtitleTempError \n} from '@/types'\n\nasync function createEditingSession() {\n  const workspaceId = createWorkspaceId()\n  const sessionConfig = {\n    workspaceId,\n    sessionType: 'editing' as const,\n    autoSaveConfig: {\n      enabled: true,\n      intervalMs: 30000,\n      createBackups: true,\n      maxBackups: 5\n    }\n  }\n  \n  try {\n    const sessionId = await createTempStorageSession(workspaceId, sessionConfig)\n    console.log(`Created session: ${sessionId}`)\n    return sessionId\n  } catch (error) {\n    if (isSubtitleTempError(error)) {\n      console.error(`Storage error [${error.code}]: ${error.message}`)\n    }\n    throw error\n  }\n}\n```\n\n### React Hook Usage\n\n```typescript\nimport { useSubtitleTempStorage } from '@/hooks'\nimport type { WorkspaceId } from '@/types'\n\nfunction SubtitleEditor({ workspaceId }: { workspaceId: WorkspaceId }) {\n  const {\n    content,\n    hasUnsavedChanges,\n    operations: { saveContent, triggerAutoSave },\n    state: { isLoading, isSaving, error },\n    utils: { validateContent }\n  } = useSubtitleTempStorage({\n    workspaceId,\n    autoSave: {\n      enabled: true,\n      intervalMs: 30000\n    },\n    callbacks: {\n      onContentChanged: (changeCount) => {\n        console.log(`${changeCount} changes made`)\n      },\n      onError: (error) => {\n        console.error('Storage error:', error.message)\n      }\n    }\n  })\n  \n  const handleSave = async () => {\n    if (!content) return\n    \n    const validation = validateContent(content)\n    if (!validation.isValid) {\n      console.error('Validation failed:', validation.errors)\n      return\n    }\n    \n    await saveContent(content)\n  }\n  \n  return (\n    <div>\n      {isLoading && <div>Loading...</div>}\n      {error && <div>Error: {error.message}</div>}\n      {hasUnsavedChanges && (\n        <button onClick={handleSave} disabled={isSaving}>\n          {isSaving ? 'Saving...' : 'Save Changes'}\n        </button>\n      )}\n      {/* Subtitle editor UI */}\n    </div>\n  )\n}\n```\n\n### Custom Validation Rules\n\n```typescript\nimport { ValidationRule, ValidationResult } from '@/types'\n\nconst timingValidationRule: ValidationRule<SubtitleTempContent> = {\n  name: 'subtitle-timing-validation',\n  severity: 'high',\n  validator: (content) => {\n    const errors = []\n    const warnings = []\n    \n    for (let i = 0; i < content.subtitles.length - 1; i++) {\n      const current = content.subtitles[i]\n      const next = content.subtitles[i + 1]\n      \n      // Check for timing overlaps\n      if (current.endTime > next.startTime) {\n        errors.push({\n          field: `subtitles[${i}].endTime`,\n          code: 'TIMING_OVERLAP',\n          message: `Subtitle timing overlap detected`,\n          value: { current: current.endTime, next: next.startTime },\n          severity: 'high' as const,\n          fixSuggestion: `Adjust end time to ${next.startTime}`\n        })\n      }\n    }\n    \n    return { isValid: errors.length === 0, errors, warnings }\n  }\n}\n```\n\n### Performance Monitoring\n\n```typescript\nimport { createPerformanceMonitor } from '@/types'\n\nconst monitor = createPerformanceMonitor()\n\nasync function monitoredOperation() {\n  const timerId = monitor.startTimer('save-operation')\n  \n  try {\n    // Perform save operation\n    await saveContent(content)\n    \n    monitor.recordMetric('save-success', 1, { \n      contentSize: estimateStorageSize(content).toString() \n    })\n  } catch (error) {\n    monitor.recordMetric('save-error', 1, { \n      errorCode: error.code \n    })\n    throw error\n  } finally {\n    const duration = monitor.endTimer(timerId)\n    console.log(`Save operation completed in ${duration.toFixed(2)}ms`)\n  }\n}\n\n// Get performance metrics\nconst saveMetrics = monitor.getAggregatedMetrics('save-operation')\nconsole.log(`Average save time: ${saveMetrics.avg.toFixed(2)}ms`)\nconsole.log(`95th percentile: ${saveMetrics.p95.toFixed(2)}ms`)\n```\n\n## Configuration\n\n### Storage Configuration\n\n```typescript\nimport { SubtitleTempStorageConfig, DEFAULT_SUBTITLE_TEMP_CONFIG } from '@/types'\n\nconst customConfig: SubtitleTempStorageConfig = {\n  ...DEFAULT_SUBTITLE_TEMP_CONFIG,\n  autoSave: {\n    ...DEFAULT_SUBTITLE_TEMP_CONFIG.autoSave,\n    intervalMs: 15000, // Faster auto-save\n    maxBackups: 10     // More backups\n  },\n  performance: {\n    ...DEFAULT_SUBTITLE_TEMP_CONFIG.performance,\n    maxConcurrentOperations: 8 // Higher concurrency\n  }\n}\n```\n\n### Auto-save Configuration\n\n```typescript\nimport { SubtitleAutoSaveConfig } from '@/types'\n\nconst autoSaveConfig: SubtitleAutoSaveConfig = {\n  enabled: true,\n  intervalMs: 30000,           // Save every 30 seconds\n  saveOnIdle: true,\n  idleTimeoutMs: 300000,       // 5 minutes idle timeout\n  saveOnChangeCount: 10,       // Save after 10 changes\n  createBackups: true,\n  maxBackups: 5,\n  compressionEnabled: true,\n  validateBeforeSave: true\n}\n```\n\n## Error Handling\n\n### Error Types\n\nAll errors are typed with specific error codes:\n\n```typescript\nexport interface SubtitleTempError extends Error {\n  code: \n    | 'STORAGE_UNAVAILABLE'\n    | 'DATA_CORRUPTION'\n    | 'VALIDATION_FAILED'\n    | 'QUOTA_EXCEEDED'\n    | 'SESSION_EXPIRED'\n    | 'HASH_MISMATCH'\n    | 'SCHEMA_VIOLATION'\n    | 'COMPRESSION_FAILED'\n  message: string\n  workspaceId?: WorkspaceId\n  sessionId?: SessionId\n  context?: any\n  timestamp: number\n  severity: 'low' | 'medium' | 'high' | 'critical'\n  recoverySuggestions?: string[]\n}\n```\n\n### Error Handling Pattern\n\n```typescript\ntry {\n  await performStorageOperation()\n} catch (error) {\n  if (isSubtitleTempError(error)) {\n    switch (error.code) {\n      case 'STORAGE_UNAVAILABLE':\n        // Handle storage unavailable\n        await fallbackToCache()\n        break\n      case 'DATA_CORRUPTION':\n        // Handle data corruption\n        await restoreFromBackup()\n        break\n      case 'VALIDATION_FAILED':\n        // Handle validation failure\n        showValidationErrors(error.context?.validationErrors)\n        break\n      default:\n        // Handle other errors\n        showGenericError(error)\n    }\n  }\n}\n```\n\n## Testing\n\n### Mock Provider\n\n```typescript\nimport { MockTempStorageProvider } from '@/types'\n\nconst mockProvider: MockTempStorageProvider = {\n  createMockSession: (workspaceId, overrides) => ({\n    sessionId: 'mock-session-id',\n    workspaceId,\n    sessionType: 'editing',\n    ...overrides\n  }),\n  \n  createMockContent: (sessionId, subtitleCount = 5) => ({\n    metadata: { id: 'mock-content', sessionId },\n    subtitles: Array.from({ length: subtitleCount }, (_, i) => ({\n      id: i + 1,\n      startTime: i * 2,\n      endTime: (i + 1) * 2,\n      text: `Subtitle ${i + 1}`,\n      confidence: 0.9\n    })),\n    // ... other required fields\n  }),\n  \n  simulateDelay: (min, max) => \n    new Promise(resolve => \n      setTimeout(resolve, Math.random() * (max - min) + min)\n    ),\n  \n  reset: () => {\n    // Reset mock state\n  }\n}\n```\n\n### Test Utilities\n\n```typescript\nimport { TempStorageTestUtils } from '@/types'\n\nconst testUtils: TempStorageTestUtils = {\n  createTestWorkspace: async (options) => {\n    const workspaceId = createWorkspaceId()\n    // Setup test workspace\n    return workspaceId\n  },\n  \n  generateTestData: {\n    subtitles: (count) => Array.from({ length: count }, (_, i) => ({\n      id: i + 1,\n      startTime: i * 2,\n      endTime: (i + 1) * 2,\n      text: `Test subtitle ${i + 1}`,\n      confidence: 0.8 + Math.random() * 0.2\n    })),\n    \n    session: (workspaceId) => ({\n      sessionId: createSessionId(),\n      workspaceId,\n      sessionType: 'editing',\n      // ... other fields\n    })\n  },\n  \n  performance: {\n    measureOperation: async (operation, iterations = 1) => {\n      const metrics = []\n      for (let i = 0; i < iterations; i++) {\n        const start = performance.now()\n        const result = await operation()\n        const duration = performance.now() - start\n        metrics.push({ operation: 'test', value: duration, unit: 'ms' })\n      }\n      return { result: await operation(), metrics }\n    }\n  }\n}\n```\n\n## Migration and Compatibility\n\n### Version Migration\n\n```typescript\nimport { MigrationStrategy } from '@/types'\n\nconst migrationV1toV2: MigrationStrategy<OldFormat, NewFormat> = {\n  version: 1,\n  targetVersion: 2,\n  description: 'Migrate from v1 to v2 format',\n  \n  up: async (oldData) => {\n    // Transform old format to new format\n    return {\n      ...oldData,\n      newField: 'default value',\n      renamedField: oldData.oldField\n    }\n  },\n  \n  down: async (newData) => {\n    // Transform new format back to old format\n    return {\n      ...newData,\n      oldField: newData.renamedField\n    }\n  },\n  \n  validate: (data) => {\n    // Validate data format\n    return { isValid: true, errors: [], warnings: [] }\n  }\n}\n```\n\n## Export/Import\n\n### Export Data\n\n```typescript\nimport { TempStorageExport } from '@/types'\n\nconst exportData: TempStorageExport = {\n  metadata: {\n    version: '2.0',\n    exportedAt: Date.now(),\n    workspaceId: 'workspace-123',\n    exportType: 'full'\n  },\n  sessions: [/* session data */],\n  content: {/* content by session ID */},\n  configs: {/* configs by session ID */}\n}\n\nconst exportJson = JSON.stringify(exportData, null, 2)\n```\n\n### Import Data\n\n```typescript\nimport { TempStorageImportOptions, TempStorageImportResult } from '@/types'\n\nconst importOptions: TempStorageImportOptions = {\n  workspaceId: 'target-workspace-id',\n  strategy: 'merge',\n  validation: {\n    strict: true,\n    skipInvalid: false,\n    reportErrors: true\n  },\n  transform: {\n    sessionIds: 'regenerate',\n    timestamps: 'update',\n    metadata: 'update'\n  }\n}\n\nconst importResult: TempStorageImportResult = await importTempStorageData(\n  exportJson,\n  importOptions\n)\n\nif (importResult.success) {\n  console.log(`Imported ${importResult.statistics.sessionsImported} sessions`)\n} else {\n  console.error('Import failed:', importResult.errors)\n}\n```\n\n## Best Practices\n\n### 1. Always Use Type Guards\n\n```typescript\n// Good\nif (isSubtitleTempContent(data)) {\n  // TypeScript knows data is SubtitleTempContent\n  processContent(data)\n}\n\n// Bad\nprocessContent(data as SubtitleTempContent)\n```\n\n### 2. Handle Errors Properly\n\n```typescript\n// Good\ntry {\n  await storageOperation()\n} catch (error) {\n  if (isSubtitleTempError(error)) {\n    handleStorageError(error)\n  } else {\n    handleUnknownError(error)\n  }\n}\n\n// Bad\ntry {\n  await storageOperation()\n} catch (error) {\n  console.log(error) // Too generic\n}\n```\n\n### 3. Use Branded Types\n\n```typescript\n// Good\nconst workspaceId = createWorkspaceId()\nconst sessionId = createSessionId()\nawait createSession(workspaceId, sessionId)\n\n// Bad\nconst workspaceId = 'some-id'\nconst sessionId = 'another-id'\nawait createSession(sessionId, workspaceId) // Wrong order!\n```\n\n### 4. Validate Before Operations\n\n```typescript\n// Good\nconst validation = validateContent(content)\nif (!validation.isValid) {\n  showErrors(validation.errors)\n  return\n}\nawait saveContent(content)\n\n// Bad\nawait saveContent(content) // May fail at runtime\n```\n\n### 5. Monitor Performance\n\n```typescript\n// Good\nconst monitor = createPerformanceMonitor()\nconst timerId = monitor.startOperation('save')\ntry {\n  await saveContent(content)\n} finally {\n  monitor.endOperation(timerId)\n}\n\n// Bad\nawait saveContent(content) // No performance tracking\n```\n\n## Contributing\n\nWhen extending the type system:\n\n1. **Add comprehensive JSDoc comments** for all new types and functions\n2. **Include usage examples** in the examples file\n3. **Create type guards** for complex types\n4. **Add validation rules** where appropriate\n5. **Update this documentation** with new features\n6. **Write tests** for new functionality\n\n## Changelog\n\n### Version 2.0\n- Added branded types for enhanced type safety\n- Implemented comprehensive validation system\n- Added performance monitoring interfaces\n- Created React hook integration types\n- Added storage bridge system\n- Implemented advanced error handling\n- Added testing utilities and mock providers\n- Created export/import system\n\n### Version 1.0\n- Initial type definitions\n- Basic storage interfaces\n- Core error types\n- Configuration types"