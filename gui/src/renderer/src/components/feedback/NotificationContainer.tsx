import React from 'react'

// TODO: Update to use new Zustand stores for notifications
// Placeholder implementation during workspace system migration

export const NotificationContainer = () => {
  // For now, return null until notification system is integrated with new stores
  return null;
};