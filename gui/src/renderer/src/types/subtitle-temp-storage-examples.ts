/**
 * Subtitle Temporary Storage Usage Examples and Documentation
 * 
 * Comprehensive examples and usage patterns for the enhanced subtitle auto-save system.
 * This file provides practical examples for developers to understand how to use
 * the type-safe temporary storage system effectively.
 */

import type {
  SubtitleData,
  ProcessingStatistics
} from '../../../types'
import type {
  SubtitleTempContent,
  SubtitleTempSession,
  SubtitleTempMetadata,
  SubtitleTempError,
  SubtitleTempOperationRequest,
  SubtitleTempStorageConfig,
  SubtitleAutoSaveConfig,
  ValidationResult
} from './subtitle-temp-storage'
import type {
  WorkspaceId,
  SessionId,
  StorageId,
  TypedOperationResult,
  ValidationRule,
  PerformanceTracker
} from './subtitle-temp-storage-utils'
import type {
  UseSubtitleTempStorageOptions,
  UseSubtitleTempStorageResult,
  SubtitleStorageBridge,
  SynchronizationResult
} from './subtitle-temp-storage-integration'

// ============================================================================
// BASIC USAGE EXAMPLES
// ============================================================================

/**
 * Example 1: Creating a basic temporary storage session
 * 
 * This example shows how to create a new temporary storage session
 * with proper type safety and error handling.
 */
export async function createBasicTempStorageSession(): Promise<SessionId> {
  const workspaceId = 'workspace-123' as WorkspaceId
  
  // Create session configuration with type safety
  const sessionConfig: Partial<SubtitleTempSession> = {
    workspaceId,
    sessionType: 'editing',
    autoSaveConfig: {
      enabled: true,
      intervalMs: 30000, // 30 seconds
      saveOnIdle: true,
      idleTimeoutMs: 300000, // 5 minutes
      saveOnChangeCount: 10,
      createBackups: true,
      maxBackups: 5,
      compressionEnabled: true,
      validateBeforeSave: true
    },
    state: {
      editingMode: 'advanced',
      multiSelect: {
        enabled: true,
        selectedIds: [],
        anchorId: undefined
      },
      undoRedoState: {
        undoStackSize: 0,
        redoStackSize: 0,
        canUndo: false,
        canRedo: false,
        lastActionTimestamp: Date.now()
      },
      pendingChanges: {
        hasChanges: false,
        changeCount: 0,
        lastChangeTimestamp: Date.now(),
        needsValidation: false
      }
    }
  }
  
  // Create session with proper error handling
  try {
    const sessionId = await createTempStorageSession(workspaceId, sessionConfig)
    console.log(`Created session: ${sessionId}`)
    return sessionId
  } catch (error) {
    if (isSubtitleTempError(error)) {
      console.error(`Storage error [${error.code}]: ${error.message}`)
      throw error
    }
    throw new Error('Unknown error creating session')
  }\n}\n\n/**\n * Example 2: Working with temporary content\n * \n * This example demonstrates how to create, validate, and manipulate\n * temporary subtitle content with full type safety.\n */\nexport function createTempContentExample(): SubtitleTempContent {\n  // Sample subtitle data\n  const subtitles: SubtitleData[] = [\n    {\n      id: 1,\n      startTime: 0,\n      endTime: 2.5,\n      text: \"Hello, welcome to our video.\",\n      translation: \"你好，歡迎觀看我們的影片。\",\n      confidence: 0.95,\n      speaker: \"Narrator\",\n      isMusic: false\n    },\n    {\n      id: 2,\n      startTime: 3.0,\n      endTime: 6.2,\n      text: \"Today we'll explore advanced features.\",\n      translation: \"今天我們將探索進階功能。\",\n      confidence: 0.88,\n      speaker: \"Narrator\",\n      isMusic: false\n    }\n  ]\n  \n  // Create temporary content with comprehensive metadata\n  const tempContent: SubtitleTempContent = {\n    metadata: {\n      id: 'temp-content-001',\n      workspaceId: 'workspace-123' as WorkspaceId,\n      sessionId: 'session-456' as SessionId,\n      version: 1,\n      schemaVersion: 1\n    },\n    subtitles,\n    statistics: {\n      totalCount: subtitles.length,\n      modifiedCount: 0,\n      totalDuration: 6.2,\n      contentCoverage: 100,\n      averageConfidence: 0.915,\n      textStats: {\n        totalCharacters: subtitles.reduce((sum, s) => sum + s.text.length, 0),\n        totalWords: subtitles.reduce((sum, s) => sum + s.text.split(' ').length, 0),\n        averageWordsPerSubtitle: 0\n      },\n      qualityMetrics: {\n        highConfidenceCount: 2,\n        mediumConfidenceCount: 0,\n        lowConfidenceCount: 0,\n        untranslatedCount: 0,\n        emptyTextCount: 0\n      },\n      timingAnalysis: {\n        averageDuration: 2.85,\n        minDuration: 2.5,\n        maxDuration: 3.2,\n        gapCount: 1,\n        overlapCount: 0,\n        totalGapDuration: 0.5\n      }\n    },\n    editingContext: {\n      currentPosition: {\n        subtitleIndex: 0,\n        cursorPosition: 0\n      },\n      selectedIds: [],\n      filters: {\n        showOnlyModified: false,\n        showOnlyUntranslated: false,\n        showOnlyLowConfidence: false\n      },\n      viewState: {\n        scrollPosition: 0,\n        zoomLevel: 1.0,\n        displayMode: 'list',\n        showConfidence: true,\n        showTimings: true\n      }\n    },\n    changeTracking: {\n      changeCount: 0,\n      lastUserAction: Date.now(),\n      modifiedIds: new Set<number>(),\n      changeSeverity: 'minor'\n    },\n    validation: {\n      isValid: true,\n      lastValidated: Date.now(),\n      warnings: [],\n      errors: [],\n      integrityScore: 1.0\n    }\n  }\n  \n  // Calculate derived statistics\n  tempContent.statistics.textStats.averageWordsPerSubtitle = \n    tempContent.statistics.textStats.totalWords / tempContent.statistics.totalCount\n  \n  return tempContent\n}\n\n/**\n * Example 3: Advanced validation with custom rules\n * \n * This example shows how to create custom validation rules\n * and compose them for comprehensive content validation.\n */\nexport function createAdvancedValidationExample(): ValidationRule<SubtitleTempContent> {\n  const contentValidationRule: ValidationRule<SubtitleTempContent> = {\n    name: 'comprehensive-content-validation',\n    severity: 'high',\n    async: false,\n    dependsOn: [],\n    validator: (content: SubtitleTempContent, context?: any): ValidationResult => {\n      const errors: ValidationError[] = []\n      const warnings: ValidationWarning[] = []\n      const suggestions: ValidationSuggestion[] = []\n      \n      // Validate subtitle timing consistency\n      for (let i = 0; i < content.subtitles.length - 1; i++) {\n        const current = content.subtitles[i]\n        const next = content.subtitles[i + 1]\n        \n        // Check for timing overlaps\n        if (current.endTime > next.startTime) {\n          errors.push({\n            field: `subtitles[${i}].endTime`,\n            code: 'TIMING_OVERLAP',\n            message: `Subtitle timing overlap detected between items ${i} and ${i + 1}`,\n            value: { current: current.endTime, next: next.startTime },\n            severity: 'high',\n            context: { subtitleIds: [current.id, next.id] },\n            fixSuggestion: `Adjust end time of subtitle ${current.id} to ${next.startTime}`\n          })\n        }\n        \n        // Check for excessive gaps\n        const gap = next.startTime - current.endTime\n        if (gap > 5.0) {\n          warnings.push({\n            field: `subtitles[${i}].gap`,\n            code: 'EXCESSIVE_GAP',\n            message: `Large gap (${gap.toFixed(2)}s) detected between subtitles`,\n            value: gap,\n            severity: 'medium',\n            context: { subtitleIds: [current.id, next.id] },\n            suggestion: 'Review if this gap is intentional'\n          })\n        }\n      }\n      \n      // Validate confidence scores\n      const lowConfidenceSubtitles = content.subtitles.filter(s => \n        s.confidence !== undefined && s.confidence < 0.7\n      )\n      \n      if (lowConfidenceSubtitles.length > 0) {\n        warnings.push({\n          field: 'subtitles.confidence',\n          code: 'LOW_CONFIDENCE_DETECTED',\n          message: `${lowConfidenceSubtitles.length} subtitles have low confidence scores`,\n          value: lowConfidenceSubtitles.length,\n          severity: 'medium',\n          context: { \n            subtitleIds: lowConfidenceSubtitles.map(s => s.id),\n            averageConfidence: lowConfidenceSubtitles.reduce((sum, s) => \n              sum + (s.confidence || 0), 0) / lowConfidenceSubtitles.length\n          },\n          suggestion: 'Review and manually verify low confidence subtitles'\n        })\n      }\n      \n      // Check for missing translations\n      const untranslatedSubtitles = content.subtitles.filter(s => \n        !s.translation || s.translation.trim() === ''\n      )\n      \n      if (untranslatedSubtitles.length > 0) {\n        suggestions.push({\n          field: 'subtitles.translation',\n          currentValue: untranslatedSubtitles.length,\n          suggestedValue: 0,\n          reason: 'All subtitles should have translations for better accessibility',\n          confidence: 0.8,\n          autoApplicable: false\n        })\n      }\n      \n      return {\n        isValid: errors.length === 0,\n        errors,\n        warnings,\n        suggestions\n      }\n    }\n  }\n  \n  return contentValidationRule\n}\n\n// ============================================================================\n// REACT HOOK USAGE EXAMPLES\n// ============================================================================\n\n/**\n * Example 4: Using the subtitle temporary storage React hook\n * \n * This example demonstrates how to use the React hook for managing\n * temporary subtitle storage in a component.\n */\nexport function useSubtitleTempStorageExample(): UseSubtitleTempStorageResult {\n  const workspaceId = 'workspace-123' as WorkspaceId\n  \n  const options: UseSubtitleTempStorageOptions = {\n    workspaceId,\n    sessionConfig: {\n      sessionType: 'editing',\n      autoStart: true,\n      inheritFromPrevious: false\n    },\n    autoSave: {\n      enabled: true,\n      intervalMs: 30000,\n      saveOnIdle: true,\n      createBackups: true,\n      maxBackups: 5\n    },\n    sync: {\n      enabled: true,\n      direction: 'bidirectional',\n      interval: 60000, // 1 minute\n      conflictResolution: 'prefer-temp'\n    },\n    performance: {\n      enableMetrics: true,\n      metricsInterval: 10000,\n      cacheEnabled: true,\n      cacheTTL: 300000 // 5 minutes\n    },\n    callbacks: {\n      onSessionCreated: (sessionId: SessionId) => {\n        console.log(`Session created: ${sessionId}`)\n      },\n      onContentChanged: (changeCount: number) => {\n        console.log(`Content changed: ${changeCount} modifications`)\n      },\n      onAutoSaveTriggered: (reason: string) => {\n        console.log(`Auto-save triggered: ${reason}`)\n      },\n      onSyncCompleted: (result: SynchronizationResult) => {\n        console.log(`Sync completed: ${result.operations.length} operations`)\n      },\n      onError: (error: SubtitleTempError) => {\n        console.error(`Storage error: ${error.message}`)\n      }\n    }\n  }\n  \n  // This would be the actual hook call in a React component\n  // const result = useSubtitleTempStorage(options)\n  \n  // Mock result for demonstration\n  const mockResult: UseSubtitleTempStorageResult = {\n    session: null,\n    sessionId: null,\n    isSessionActive: false,\n    content: null,\n    isContentLoaded: false,\n    hasUnsavedChanges: false,\n    changeCount: 0,\n    operations: {\n      saveContent: async (content: SubtitleTempContent) => {\n        console.log('Saving content...')\n      },\n      loadContent: async (sessionId?: SessionId) => {\n        console.log('Loading content...')\n        return null\n      },\n      createSession: async (sessionType?: string) => {\n        console.log('Creating session...')\n        return 'new-session-id' as SessionId\n      },\n      endSession: async (sessionId?: SessionId) => {\n        console.log('Ending session...')\n      },\n      triggerAutoSave: async () => {\n        console.log('Triggering auto-save...')\n      },\n      synchronize: async (options?) => {\n        console.log('Synchronizing...')\n        return {\n          operations: [],\n          conflicts: [],\n          metrics: {\n            itemsProcessed: 0,\n            conflictsResolved: 0,\n            duration: 0,\n            bytesTransferred: 0\n          },\n          finalState: {\n            temporaryItemCount: 0,\n            persistentItemCount: 0,\n            lastSyncTimestamp: Date.now()\n          }\n        }\n      }\n    },\n    state: {\n      isLoading: false,\n      isSaving: false,\n      isSyncing: false,\n      lastSaveTime: null,\n      lastSyncTime: null,\n      error: null\n    },\n    metrics: {\n      operationCount: 0,\n      averageOperationTime: 0,\n      cacheHitRate: 0,\n      errorCount: 0,\n      totalDataProcessed: 0\n    },\n    config: {\n      autoSaveConfig: options.autoSave || {\n        enabled: false,\n        intervalMs: 30000,\n        saveOnIdle: false,\n        idleTimeoutMs: 300000,\n        saveOnChangeCount: 10,\n        createBackups: false,\n        maxBackups: 5,\n        compressionEnabled: false,\n        validateBeforeSave: false\n      },\n      updateAutoSaveConfig: async (config) => {\n        console.log('Updating auto-save config...')\n      }\n    },\n    utils: {\n      validateContent: (content: SubtitleTempContent) => {\n        return {\n          isValid: true,\n          errors: [],\n          warnings: []\n        }\n      },\n      estimateStorageSize: (content: SubtitleTempContent) => {\n        return JSON.stringify(content).length\n      },\n      clearCache: async () => {\n        console.log('Clearing cache...')\n      },\n      exportSession: async () => {\n        return JSON.stringify({ session: 'data' })\n      },\n      importSession: async (sessionData: string) => {\n        console.log('Importing session data...')\n      }\n    }\n  }\n  \n  return mockResult\n}\n\n// ============================================================================\n// ADVANCED INTEGRATION EXAMPLES\n// ============================================================================\n\n/**\n * Example 5: Storage Bridge Implementation\n * \n * This example shows how to implement a storage bridge for\n * synchronization between temporary and persistent storage.\n */\nexport class ExampleStorageBridge implements SubtitleStorageBridge {\n  /**\n   * Convert temporary content to persistent file content\n   */\n  toPersistentContent(tempContent: SubtitleTempContent): SubtitleFileContent {\n    return {\n      metadata: {\n        fileId: `file-${tempContent.metadata.id}`,\n        workspaceId: tempContent.metadata.workspaceId,\n        version: tempContent.metadata.version,\n        schemaVersion: tempContent.metadata.schemaVersion\n      },\n      subtitles: tempContent.subtitles,\n      statistics: {\n        totalSubtitles: tempContent.statistics.totalCount,\n        totalDuration: tempContent.statistics.totalDuration,\n        wordCount: tempContent.statistics.textStats.totalWords,\n        characterCount: tempContent.statistics.textStats.totalCharacters,\n        translationCoverage: tempContent.statistics.contentCoverage,\n        averageConfidence: tempContent.statistics.averageConfidence,\n        speakerDistribution: {},\n        musicSegments: 0,\n        qualityDistribution: {\n          high: tempContent.statistics.qualityMetrics.highConfidenceCount,\n          medium: tempContent.statistics.qualityMetrics.mediumConfidenceCount,\n          low: tempContent.statistics.qualityMetrics.lowConfidenceCount\n        },\n        timingStats: {\n          averageDuration: tempContent.statistics.timingAnalysis.averageDuration,\n          minDuration: tempContent.statistics.timingAnalysis.minDuration,\n          maxDuration: tempContent.statistics.timingAnalysis.maxDuration,\n          gapCount: tempContent.statistics.timingAnalysis.gapCount,\n          overlapCount: tempContent.statistics.timingAnalysis.overlapCount\n        }\n      },\n      editHistory: [],\n      validation: {\n        isValid: tempContent.validation.isValid,\n        warnings: tempContent.validation.warnings.map(w => w.message),\n        errors: tempContent.validation.errors.map(e => e.message),\n        qualityScore: tempContent.validation.integrityScore\n      }\n    }\n  }\n  \n  /**\n   * Convert persistent file content to temporary content\n   */\n  toTemporaryContent(fileContent: SubtitleFileContent, sessionId: SessionId): SubtitleTempContent {\n    return {\n      metadata: {\n        id: `temp-${fileContent.metadata.fileId}`,\n        workspaceId: fileContent.metadata.workspaceId,\n        sessionId,\n        version: fileContent.metadata.version,\n        schemaVersion: fileContent.metadata.schemaVersion\n      },\n      subtitles: fileContent.subtitles,\n      statistics: {\n        totalCount: fileContent.statistics.totalSubtitles,\n        modifiedCount: 0,\n        totalDuration: fileContent.statistics.totalDuration,\n        contentCoverage: fileContent.statistics.translationCoverage,\n        averageConfidence: fileContent.statistics.averageConfidence,\n        textStats: {\n          totalCharacters: fileContent.statistics.characterCount,\n          totalWords: fileContent.statistics.wordCount,\n          averageWordsPerSubtitle: fileContent.statistics.wordCount / fileContent.statistics.totalSubtitles\n        },\n        qualityMetrics: {\n          highConfidenceCount: fileContent.statistics.qualityDistribution.high,\n          mediumConfidenceCount: fileContent.statistics.qualityDistribution.medium,\n          lowConfidenceCount: fileContent.statistics.qualityDistribution.low,\n          untranslatedCount: 0,\n          emptyTextCount: 0\n        },\n        timingAnalysis: {\n          averageDuration: fileContent.statistics.timingStats.averageDuration,\n          minDuration: fileContent.statistics.timingStats.minDuration,\n          maxDuration: fileContent.statistics.timingStats.maxDuration,\n          gapCount: fileContent.statistics.timingStats.gapCount,\n          overlapCount: fileContent.statistics.timingStats.overlapCount,\n          totalGapDuration: 0\n        }\n      },\n      editingContext: {\n        selectedIds: [],\n        filters: {\n          showOnlyModified: false,\n          showOnlyUntranslated: false,\n          showOnlyLowConfidence: false\n        },\n        viewState: {\n          scrollPosition: 0,\n          zoomLevel: 1.0,\n          displayMode: 'list',\n          showConfidence: true,\n          showTimings: true\n        }\n      },\n      changeTracking: {\n        changeCount: 0,\n        lastUserAction: Date.now(),\n        modifiedIds: new Set<number>(),\n        changeSeverity: 'minor'\n      },\n      validation: {\n        isValid: fileContent.validation.isValid,\n        lastValidated: Date.now(),\n        warnings: [],\n        errors: [],\n        integrityScore: fileContent.validation.qualityScore\n      }\n    }\n  }\n  \n  /**\n   * Synchronize changes between temporary and persistent storage\n   */\n  async synchronize(\n    workspaceId: WorkspaceId,\n    sessionId: SessionId,\n    options?: {\n      direction: 'temp-to-persistent' | 'persistent-to-temp' | 'bidirectional'\n      conflictResolution: 'prefer-temp' | 'prefer-persistent' | 'manual'\n      createBackup: boolean\n    }\n  ): Promise<TypedOperationResult<SynchronizationResult>> {\n    const startTime = Date.now()\n    \n    try {\n      // Implementation would go here\n      const result: SynchronizationResult = {\n        operations: [],\n        conflicts: [],\n        metrics: {\n          itemsProcessed: 0,\n          conflictsResolved: 0,\n          duration: Date.now() - startTime,\n          bytesTransferred: 0\n        },\n        finalState: {\n          temporaryItemCount: 0,\n          persistentItemCount: 0,\n          lastSyncTimestamp: Date.now()\n        }\n      }\n      \n      return {\n        success: true,\n        operationId: 'sync-operation-001' as OperationId,\n        workspaceId,\n        sessionId,\n        data: result,\n        timestamp: Date.now(),\n        metadata: {\n          duration: Date.now() - startTime,\n          bytesProcessed: 0,\n          cacheHit: false,\n          retryCount: 0\n        }\n      }\n    } catch (error) {\n      return {\n        success: false,\n        operationId: 'sync-operation-001' as OperationId,\n        workspaceId,\n        sessionId,\n        error: error as SubtitleTempError,\n        timestamp: Date.now(),\n        metadata: {\n          duration: Date.now() - startTime,\n          bytesProcessed: 0,\n          cacheHit: false,\n          retryCount: 0\n        }\n      }\n    }\n  }\n  \n  /**\n   * Validate compatibility between storage formats\n   */\n  validateCompatibility(\n    tempContent: SubtitleTempContent,\n    fileContent: SubtitleFileContent\n  ): ValidationResult {\n    const errors: ValidationError[] = []\n    const warnings: ValidationWarning[] = []\n    \n    // Check schema version compatibility\n    if (tempContent.metadata.schemaVersion !== fileContent.metadata.schemaVersion) {\n      warnings.push({\n        field: 'schemaVersion',\n        code: 'SCHEMA_VERSION_MISMATCH',\n        message: 'Schema versions do not match between temporary and persistent storage',\n        value: {\n          temp: tempContent.metadata.schemaVersion,\n          persistent: fileContent.metadata.schemaVersion\n        },\n        severity: 'medium'\n      })\n    }\n    \n    // Check subtitle count consistency\n    if (tempContent.subtitles.length !== fileContent.subtitles.length) {\n      errors.push({\n        field: 'subtitles.length',\n        code: 'SUBTITLE_COUNT_MISMATCH',\n        message: 'Subtitle counts do not match between storage formats',\n        value: {\n          temp: tempContent.subtitles.length,\n          persistent: fileContent.subtitles.length\n        },\n        severity: 'high'\n      })\n    }\n    \n    return {\n      isValid: errors.length === 0,\n      errors,\n      warnings\n    }\n  }\n}\n\n// ============================================================================\n// PERFORMANCE MONITORING EXAMPLES\n// ============================================================================\n\n/**\n * Example 6: Performance tracking implementation\n * \n * This example demonstrates how to implement performance tracking\n * for subtitle temporary storage operations.\n */\nexport class ExamplePerformanceTracker implements PerformanceTracker {\n  private operations = new Map<string, number>()\n  private metrics: PerformanceMetric[] = []\n  \n  startOperation(name: string, context?: OperationContext): string {\n    const operationId = `${name}-${Date.now()}-${Math.random().toString(36).substring(2, 8)}`\n    this.operations.set(operationId, performance.now())\n    \n    // Log operation start\n    console.log(`[Performance] Started operation: ${name} (${operationId})`)\n    \n    return operationId\n  }\n  \n  endOperation(id: string): number {\n    const startTime = this.operations.get(id)\n    if (!startTime) {\n      console.warn(`[Performance] Operation ${id} not found`)\n      return 0\n    }\n    \n    const duration = performance.now() - startTime\n    this.operations.delete(id)\n    \n    // Record metric\n    const [operationName] = id.split('-')\n    this.recordMetric(operationName, duration, { operationId: id })\n    \n    console.log(`[Performance] Completed operation: ${operationName} in ${duration.toFixed(2)}ms`)\n    \n    return duration\n  }\n  \n  recordMetric(name: string, value: number, tags?: Record<string, string>): void {\n    const metric: PerformanceMetric = {\n      id: `metric-${Date.now()}-${Math.random().toString(36).substring(2, 6)}`,\n      operation: name,\n      value,\n      unit: 'ms',\n      timestamp: Date.now(),\n      tags\n    }\n    \n    this.metrics.push(metric)\n    \n    // Keep only last 1000 metrics\n    if (this.metrics.length > 1000) {\n      this.metrics = this.metrics.slice(-1000)\n    }\n  }\n  \n  getMetrics(filter?: string): PerformanceMetric[] {\n    if (filter) {\n      return this.metrics.filter(m => m.operation === filter)\n    }\n    return [...this.metrics]\n  }\n  \n  getAggregatedMetrics(operation: string): AggregatedMetrics {\n    const operationMetrics = this.metrics.filter(m => m.operation === operation)\n    \n    if (operationMetrics.length === 0) {\n      return {\n        operation,\n        count: 0,\n        min: 0,\n        max: 0,\n        avg: 0,\n        median: 0,\n        p95: 0,\n        p99: 0,\n        total: 0,\n        rate: 0,\n        errorRate: 0\n      }\n    }\n    \n    const values = operationMetrics.map(m => m.value).sort((a, b) => a - b)\n    const count = values.length\n    const total = values.reduce((sum, v) => sum + v, 0)\n    \n    return {\n      operation,\n      count,\n      min: values[0],\n      max: values[count - 1],\n      avg: total / count,\n      median: values[Math.floor(count / 2)],\n      p95: values[Math.floor(count * 0.95)],\n      p99: values[Math.floor(count * 0.99)],\n      total,\n      rate: count / ((Date.now() - operationMetrics[0].timestamp) / 1000),\n      errorRate: 0 // Would be calculated based on error metrics\n    }\n  }\n}\n\n// ============================================================================\n// CONFIGURATION EXAMPLES\n// ============================================================================\n\n/**\n * Example 7: Advanced configuration setup\n * \n * This example shows how to create and validate advanced\n * configuration for the subtitle temporary storage system.\n */\nexport function createAdvancedStorageConfig(): SubtitleTempStorageConfig {\n  const config: SubtitleTempStorageConfig = {\n    database: {\n      name: 'CantoCap_SubtitleTemp_Advanced',\n      version: 2,\n      maxSize: 200 * 1024 * 1024, // 200MB\n      cleanupInterval: 1800000 // 30 minutes\n    },\n    autoSave: {\n      enabled: true,\n      intervalMs: 15000, // 15 seconds for faster saves\n      saveOnIdle: true,\n      idleTimeoutMs: 180000, // 3 minutes\n      saveOnChangeCount: 5, // Save after 5 changes\n      createBackups: true,\n      maxBackups: 15, // More backups for important work\n      compressionEnabled: true,\n      validateBeforeSave: true\n    },\n    validation: {\n      enableIntegrityChecks: true,\n      validateOnLoad: true,\n      validateOnSave: true,\n      repairCorruption: true // Enable automatic repair\n    },\n    performance: {\n      enableCompression: true,\n      compressionThreshold: 512, // 512 bytes\n      maxConcurrentOperations: 10, // Higher concurrency\n      operationTimeout: 45000 // 45 seconds\n    },\n    cleanup: {\n      maxRetentionDays: 14, // 2 weeks\n      maxStorageSize: 150 * 1024 * 1024, // 150MB trigger\n      cleanupBatchSize: 50,\n      enableAutomaticCleanup: true\n    }\n  }\n  \n  // Validate the configuration\n  const validation = validateStorageConfig(config)\n  if (!validation.isValid) {\n    console.error('Configuration validation failed:', validation.errors)\n    throw new Error('Invalid storage configuration')\n  }\n  \n  if (validation.warnings.length > 0) {\n    console.warn('Configuration warnings:', validation.warnings)\n  }\n  \n  return config\n}\n\n// ============================================================================\n// ERROR HANDLING EXAMPLES\n// ============================================================================\n\n/**\n * Example 8: Comprehensive error handling\n * \n * This example demonstrates best practices for handling\n * errors in the subtitle temporary storage system.\n */\nexport async function handleStorageErrorsExample(): Promise<void> {\n  const workspaceId = 'workspace-123' as WorkspaceId\n  const sessionId = 'session-456' as SessionId\n  \n  try {\n    // Attempt to load content\n    const content = await loadTempContent(workspaceId, sessionId)\n    \n    if (!content) {\n      throw new SubtitleTempError(\n        'CONTENT_NOT_FOUND',\n        'No content found for the specified session',\n        workspaceId,\n        sessionId\n      )\n    }\n    \n    // Process content...\n    \n  } catch (error) {\n    if (isSubtitleTempError(error)) {\n      // Handle specific temporary storage errors\n      switch (error.code) {\n        case 'STORAGE_UNAVAILABLE':\n          console.error('Storage system is unavailable, trying fallback...')\n          // Implement fallback logic\n          break\n          \n        case 'DATA_CORRUPTION':\n          console.error('Data corruption detected, attempting recovery...')\n          // Implement recovery logic\n          if (error.recovery?.autoRecoverable) {\n            await attemptAutoRecovery(error)\n          } else {\n            // Notify user of manual intervention needed\n            notifyUserOfCorruption(error)\n          }\n          break\n          \n        case 'VALIDATION_FAILED':\n          console.warn('Validation failed, showing details to user...')\n          // Show validation errors to user\n          displayValidationErrors(error.context?.validationErrors || [])\n          break\n          \n        case 'QUOTA_EXCEEDED':\n          console.warn('Storage quota exceeded, triggering cleanup...')\n          await triggerStorageCleanup(workspaceId)\n          break\n          \n        default:\n          console.error(`Unhandled storage error [${error.code}]: ${error.message}`)\n          // Generic error handling\n          handleGenericStorageError(error)\n      }\n    } else if (error instanceof Error) {\n      console.error('Unexpected error:', error.message)\n      // Handle unexpected errors\n    } else {\n      console.error('Unknown error occurred:', error)\n    }\n  }\n}\n\n// ============================================================================\n// HELPER FUNCTIONS FOR EXAMPLES\n// ============================================================================\n\n// Mock functions that would be implemented in the actual system\ndeclare function createTempStorageSession(\n  workspaceId: WorkspaceId,\n  config: Partial<SubtitleTempSession>\n): Promise<SessionId>\n\ndeclare function loadTempContent(\n  workspaceId: WorkspaceId,\n  sessionId: SessionId\n): Promise<SubtitleTempContent | null>\n\ndeclare function isSubtitleTempError(error: any): error is SubtitleTempError\n\ndeclare function attemptAutoRecovery(error: SubtitleTempError): Promise<void>\n\ndeclare function notifyUserOfCorruption(error: SubtitleTempError): void\n\ndeclare function displayValidationErrors(errors: string[]): void\n\ndeclare function triggerStorageCleanup(workspaceId: WorkspaceId): Promise<void>\n\ndeclare function handleGenericStorageError(error: SubtitleTempError): void\n\ndeclare function validateStorageConfig(config: any): EnhancedValidationResult\n\n// Mock interfaces for examples\ninterface SubtitleFileContent {\n  metadata: {\n    fileId: string\n    workspaceId: WorkspaceId\n    version: number\n    schemaVersion: number\n  }\n  subtitles: SubtitleData[]\n  statistics: any\n  editHistory: any[]\n  validation: {\n    isValid: boolean\n    warnings: string[]\n    errors: string[]\n    qualityScore: number\n  }\n}\n\ninterface OperationContext {\n  workspaceId: WorkspaceId\n  sessionId?: SessionId\n  operationId: OperationId\n  userId?: string\n  requestTimestamp: number\n  timeout?: number\n  priority: 'low' | 'normal' | 'high' | 'critical'\n  metadata?: Record<string, any>\n}\n\ninterface AggregatedMetrics {\n  operation: string\n  count: number\n  min: number\n  max: number\n  avg: number\n  median: number\n  p95: number\n  p99: number\n  total: number\n  rate: number\n  errorRate: number\n}\n\ninterface ValidationError {\n  field: string\n  code: string\n  message: string\n  value: any\n  severity: ValidationSeverity\n  context?: Record<string, any>\n  fixSuggestion?: string\n}\n\ninterface ValidationWarning {\n  field: string\n  code: string\n  message: string\n  value: any\n  severity: Exclude<ValidationSeverity, 'critical'>\n  context?: Record<string, any>\n  suggestion?: string\n}\n\ninterface ValidationSuggestion {\n  field: string\n  currentValue: any\n  suggestedValue: any\n  reason: string\n  confidence: number\n  autoApplicable: boolean\n}\n\ninterface EnhancedValidationResult extends ValidationResult {\n  context: {\n    workspaceId: WorkspaceId\n    timestamp: number\n  }\n  metrics: {\n    duration: number\n    rulesExecuted: number\n    rulesSkipped: number\n    cacheHits: number\n  }\n  performance: {\n    validationTime: number\n    averageRuleTime: number\n    slowestRule?: string\n  }\n}\n\nclass SubtitleTempError extends Error {\n  constructor(\n    public code: string,\n    message: string,\n    public workspaceId?: WorkspaceId,\n    public sessionId?: SessionId,\n    public context?: any,\n    public recovery?: {\n      action: string\n      autoRecoverable: boolean\n    }\n  ) {\n    super(message)\n    this.name = 'SubtitleTempError'\n  }\n}"