# Enhanced Gemini Flash Integration Dependencies
# Install with: pip install -r requirements-gemini.txt

# Core Gemini Flash integration
google-generativeai>=0.8.0

# Environment file support (optional but recommended)
python-dotenv>=1.0.0

# Video processing (already included in main requirements)
# ffmpeg-python>=0.2.0

# Additional dependencies that may be useful
# Pillow>=10.0.0  # For image processing if needed
# requests>=2.31.0  # For HTTP requests (usually included)